# 环境搭建指南

## 1. 系统要求
- OS: Ubuntu 22.04 LTS
- ROS: ROS 2 Humble

## 2. 依赖安装
```bash
# 安装 ROS 2
sudo apt install ros-humble-desktop

# 安装 PX4 依赖
# ...

# 安装 Zenoh / DDS Agent
# ...
```

## 3. 编译说明
```bash
cd ~/usv_workspace
colcon build --symlink-install
source install/setup.bash
```
