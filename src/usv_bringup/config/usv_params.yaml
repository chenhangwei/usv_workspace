mavros_node:
  ros__parameters:
    fcu_url: "serial:///dev/ttyACM0:57600"
    gcs_url: ""
    tgt_system: 1
    tgt_component: 1
    vision_pose:
      enable: true   # 推荐改为嵌套结构，避免使用 . 分隔

usv_status_node:
  ros__parameters:
    dummy_param: true

usv_avoidance_node:
  ros__parameters:
    in_distance_value: 1.2

usv_command_node:
  ros__parameters:
    dummy_param: true

usv_control_node:
  ros__parameters:
    dummy_param: true

usv_uwb_node:
  ros__parameters:
    dummy_param: true

usv_led_node:
  ros__parameters:
    dummy_param: true

usv_sound_node:
  ros__parameters:
    dummy_param: true

